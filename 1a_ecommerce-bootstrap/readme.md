# 1a_ecommerce-bootstrap

## How to Run

1. Open the project folder in VS Code.
2. Right-click on the main HTML file (e.g., `index.html`).
3. Select **Open with Live Server** (requires the Live Server extension).
4. Alternatively, you can directly double-click the HTML file to open it in a web browser.
